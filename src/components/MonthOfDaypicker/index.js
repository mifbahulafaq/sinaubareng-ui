import style from './MonthOfDaypicker.module.css';
import { useDayRender, Months } from 'react-day-picker';

export default function MonthOfDaypicker(props){
	console.log(props)
	return <div>dasads</div>
}

// InputDate.propTypes = {
	// margin: PropTypes.string,
	// fontSize: PropTypes.string,
	// initialText:PropTypes.string,
	// width: PropTypes.string,
	// minDate: PropTypes.string,
	// dateInput: PropTypes.string,
	// timeInput: PropTypes.string,
	// dateRegistration: PropTypes.object.isRequired,
	// timeRegistration: PropTypes.object.isRequired,
	// active: PropTypes.bool,
// }
// InputDate.defaultProps = {
	// margin: "0",
	// fontSize: "1rem",
	// initialText: "-",
	// width: "100%",
	// minDate: "",
	// active: PropTypes.bool,
// }